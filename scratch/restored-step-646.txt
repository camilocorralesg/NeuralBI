
Now I'll completely redesign the TrustBar. The approach: instead of all 4 variations sharing the same ToolCard layout, each variation gets its own unique rendering. I'll keep tech_v4 (the one with neon cards), and radically redesign the other three.
"function TrustBar({ activeHero }) {\n  // Each hero gets a COMPLETELY different Trust Bar layout & treatment\n  switch (activeHero) {\n\n    // ─── NEBULA: Pure inline strip. No cards, no chrome. Just logos floating. ───\n    case 'spline1':\n      return (\n        <section style={{\n          padding: '3rem 0',\n          background: 'transparent',\n          position: 'relative',\n          zIndex: 20,\n        }}>\n          <div className=\"animate-blur-reveal\" style={{\n            display: 'flex',\n            justifyContent: 'center',\n            alignItems: 'center',\n            gap: '4rem',\n            flexWrap: 'wrap',\n            padding: '0 2rem',\n          }}>\n            {toolLogos.map((tool) => (\n              <InlineLogo key={tool.name} tool={tool}\n                opacity={0.25} hoverOpacity={0.7}\n                labelColor=\"rgba(255,255,255,0.0)\" hoverLabelColor=\"rgba(255,255,255,0.5)\"\n              />\n            ))}\n          </div>\n        </section>\n      );\n\n    // ─── CINEMATIC: Full-width dark strip with hairline borders and centered logos ───\n    case 'cinematic':\n      return (\n        <section style={{\n          padding: '0',\n          background: '#050505',\n          position: 'relative',\n          zIndex: 20,\n        }}>\n          {/* Top hairline */}\n          <div style={{ height: '1px', background: 'linear-gradient(90deg, transparent 15%, rgba(255,255,255,0.05) 50%, transparent 85%)' }} />\n\n          <div style={{\n            display: 'flex',\n            justifyContent: 'center',\n            alignItems: 'center',\n            gap: '0',\n            maxWidth: '100%',\n          }}>\n            {toolLogos.map((tool, i) => (\n              <React.Fragment key={tool.name}>\n                <CinematicLogo tool={tool} />\n                {i < toolLogos.length - 1 && (\n                  <div style={{ width: '1px', height: '100%', alignSelf: 'stretch', background: 'rgba(255,255,255,0.04)' }} />\n                )}\n           
<truncated 9618 bytes>